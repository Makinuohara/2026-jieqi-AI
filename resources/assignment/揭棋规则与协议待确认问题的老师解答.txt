Q1：六次重复局面。
Q2：连续六次捉子重复局面。更加精确可以规定没有走动的棋子被动捉子不算捉，比如兵始终在车口，但是大家忙于其它的重复局面就不是长捉。
Q3：是的。
Q4：也不必提示。
Q5：服务器内部的处理自行设计即可。基于终局的判定我不大理解含义，要么认输超时提和通过，要么将军被吃掉，似乎无需判断啊。
Q6：就是服务器对两边客户端发送不同type，不知道一方为null，很简单啊。
Q7：当然不能发送多余信息给客户端。
Q8：可以忽略，也可以进行验证，加强正确性检查。但是以服务器端为准，比如服务器判断此位置为车，客户端篡改无效。
Q9：从服务器发送走子或者提和信息开始计算，可加5S算作网络延迟，以服务器端计算超时。
Q10：服务器端可以用AI编程，但是却不需要使用AI能力，客户端AI和人超时一视同仁即可。非法走子、协议格式错误可以对客户端给出提示信息（若如果原来的协议中没有可以自行添加，），但计时不中断（即不能以此耍赖延时），另外
Q11:  终局前翻出来的棋子其实无需记录，未翻出来的记录类型对于分析其实没有帮助。所以自由设计即可。不过如果选择记录，可要注意棋谱终局前不能发送给客户端，不然泄密了。
Q12：不建议做本地AI对决，一台计算机上启动两个客户端即可。
Q13：当然。
Q14：可选功能都可以自己规定协议。记住你们要兼容的是最小版本（必选内容）和你自己的扩展版本。