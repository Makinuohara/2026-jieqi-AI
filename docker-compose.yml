services:
  jieqi-server:
    build:
      context: .
      dockerfile: jieqi-server/Dockerfile
    environment:
      JIEQI_SERVER_PORT: 8887
    ports:
      - "8887:8887"
    restart: unless-stopped
