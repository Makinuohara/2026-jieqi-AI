# 完整终局规则实施计划

## 目标

在 `feature/完整终局规则` 分支完成 80 半回合判和、困毙判负和将军提示。

## 实施步骤

1. [已完成] 在 `StandardGameEngineTest` 增加无吃子计数、吃子清零和困毙测试。
2. [已完成] 扩展 `GameEngine`，提供只读的将军检测接口。
3. [已完成] 在 `StandardGameEngine` 中实现攻击判断和终局裁决顺序。
4. [已完成] 在 `LocalHumanVsAiGame` 暴露当前方是否被将军。
5. [已完成] 在 `HumanVsAiView` 增加中文将军状态提示。
6. [已完成] 更新 README、开发任务和架构说明。
7. [已完成] 运行 `.\mvnw.cmd clean test` 和真实 GUI 操作验证。

## 验收标准

- 所有原有测试继续通过。
- 80 个连续无吃子半回合准确判和。
- 吃子后计数归零。
- 困毙方准确判负。
- 被将军不限制玩家选择其他合法着法。
- GUI 正确显示被将军和终局状态。
